<UserControl x:Class="BMPharma.UI.Views.ChifaBordereauStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <StackPanel>
            <TextBlock Text="CHIFA Bordereau Status" FontSize="22" FontWeight="Bold"
                       Foreground="#1B5E20" Margin="0,0,0,16" />

            <Border BorderBrush="#E0E0E0" BorderThickness="1" Padding="16" CornerRadius="4">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Next Bordereau Number:"
                               VerticalAlignment="Center" Margin="0,8" />
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="(Not connected)"
                               FontSize="14" Margin="0,8" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Current Mode:"
                               VerticalAlignment="Center" Margin="0,8" />
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="ReadOnly"
                               FontSize="14" Foreground="#FF9800" Margin="0,8" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Signing:"
                               VerticalAlignment="Center" Margin="0,8" />
                    <TextBlock Grid.Row="2" Grid.Column="1"
                               Text="Delegated to CHIFA-OFFICINE (PKCS#11 token required)"
                               FontSize="14" Foreground="#F44336" Margin="0,8" />
                </Grid>
            </Border>

            <TextBlock Text="Note: Bordereau signing is performed exclusively through CHIFA-OFFICINE using the pharmacist's professional token."
                       FontSize="12" Foreground="#757575" Margin="0,16,0,0"
                       TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</UserControl>
